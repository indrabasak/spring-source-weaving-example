server:
  port: 8080


# For Spring Actuator /info endpoint
info:
    artifact: spring-source-weaving-example
    name: spring-source-weaving-example
    description: Spring Source Weaving Example
    version: 1.0.0

#Exposes Spring actuator endpoints
management:
  health:
    diskspace:
      enabled: true
    db:
      enabled: true
    defaults:
      enabled: true
    details:
      enabled: true
    application:
      enabled: true
  endpoint:
    health:
      enabled: true
      show-details: true
  endpoints:
    web:
      base-path: /
      expose: "*"



#logging:
#  level:
#    org.springframework: DEBUG





